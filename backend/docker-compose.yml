version: '3.9'

services:
  # =============================================================================
  # Backend Application
  # =============================================================================
  backend:
    build: .
    image: rtc-backend:latest
    container_name: rtc-backend
    env_file: .env
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      mongo:
        condition: service_started
    restart: unless-stopped
    networks:
      - rtc-network

  # =============================================================================
  # PostgreSQL Database (Users, Servers, Channels, Memberships)
  # =============================================================================
  postgres:
    image: postgres:16-alpine
    container_name: rtc-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: rtc
      POSTGRES_PASSWORD: rtc_password
      POSTGRES_DB: rtc
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/migrations/001_initial_schema.sql:/docker-entrypoint-initdb.d/001_initial_schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rtc -d rtc"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rtc-network

  # =============================================================================
  # MongoDB (Messages, Real-time data)
  # =============================================================================
  mongo:
    image: mongo:6.0
    container_name: rtc-mongo
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db
    ports:
      - "27017:27017"
    networks:
      - rtc-network

  # =============================================================================
  # Adminer (Database GUI - Development only)
  # =============================================================================
  adminer:
    image: adminer:latest
    container_name: rtc-adminer
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    depends_on:
      - postgres
    networks:
      - rtc-network
    profiles:
      - dev

volumes:
  postgres_data:
  mongo_data:

networks:
  rtc-network:
    driver: bridge
